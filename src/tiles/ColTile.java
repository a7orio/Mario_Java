package tiles;

import java.awt.Graphics;
import java.awt.image.BufferedImage;

public class ColTile extends Tile{

	public ColTile(BufferedImage ssprite, int xx, int yy, boolean vvisible) {
		super(ssprite, xx, yy, vvisible);
	}

	
	public void render(Graphics g) {
		super.render(g);
	}
}
